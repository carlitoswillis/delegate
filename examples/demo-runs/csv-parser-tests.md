Write unit tests for lib/csv_parser.py.

Cover: a quoted field with an embedded comma, an empty field between two
commas, and a file with no trailing newline. Use pytest and put the new
tests in tests/test_csv_parser.py next to the existing suite.
